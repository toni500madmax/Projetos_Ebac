function multiplicacao(x: number, y: number): number {
    return x * y;
}

const saudacao = (nome: string) => {
    return `Olá ${nome}. Seja bem vindo(a).\n`;
}

console.log(multiplicacao(30, 45));
console.log(saudacao("Katharine Colon"));
